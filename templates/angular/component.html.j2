<<<<<<< HEAD
<div class="customer-management">
  <mat-toolbar color="primary" class="app-toolbar">
    <mat-toolbar-row>
      <span class="app-title">{{ project_name | title }} CRM</span>
      <span class="spacer"></span>

      <button mat-icon-button [matMenuTriggerFor]="menu" aria-label="Open menu">
        <mat-icon>more_vert</mat-icon>
      </button>
      <mat-menu #menu="matMenu">
        <button mat-menu-item (click)="startAdd()">
          <mat-icon>add</mat-icon>
          <span>Add Customer</span>
        </button>
        <button mat-menu-item disabled>
          <mat-icon>settings</mat-icon>
          <span>Settings</span>
        </button>
      </mat-menu>
    </mat-toolbar-row>
  </mat-toolbar>

  <div class="content-container">
    <mat-card class="hero-card" *ngIf="!loading && customers.length === 0 && !showAddForm">
      <mat-card-header>
        <mat-card-title>Welcome</mat-card-title>
        <mat-card-subtitle>Start by creating your first customer</mat-card-subtitle>
      </mat-card-header>
      <mat-card-content>
        <p>This starter UI ships with Angular Material, responsive layout, search, validation, and accessible components.</p>
      </mat-card-content>
      <mat-card-actions>
        <button mat-raised-button color="primary" (click)="startAdd()">
          <mat-icon>add</mat-icon>
          Add Customer
        </button>
      </mat-card-actions>
    </mat-card>

    <mat-card class="form-card" *ngIf="showAddForm">
      <mat-card-header>
        <mat-card-title [textContent]="isEditMode ? 'Edit Customer' : 'Add Customer'"></mat-card-title>
        <mat-card-subtitle [textContent]="isEditMode ? 'Update customer details' : 'Create a new customer'"></mat-card-subtitle>
      </mat-card-header>

      <mat-card-content>
        <form [formGroup]="addForm" (ngSubmit)="isEditMode ? updateCustomer() : addCustomer()" class="customer-form">
          <div class="form-row">
            <mat-form-field appearance="outline" class="form-field">
              <mat-label>Name</mat-label>
              <input matInput formControlName="name" placeholder="e.g. Alex Johnson" />
              <mat-icon matSuffix>person</mat-icon>
              <mat-error *ngIf="isFieldInvalid(addForm, 'name')" [textContent]="getFieldErrorMessage(addForm, 'name')"></mat-error>
            </mat-form-field>

            <mat-form-field appearance="outline" class="form-field">
              <mat-label>Email</mat-label>
              <input matInput type="email" formControlName="email" placeholder="alex@example.com" />
              <mat-icon matSuffix>email</mat-icon>
              <mat-error *ngIf="isFieldInvalid(addForm, 'email')" [textContent]="getFieldErrorMessage(addForm, 'email')"></mat-error>
            </mat-form-field>
          </div>

          <div class="form-row">
            <mat-form-field appearance="outline" class="form-field">
              <mat-label>Company</mat-label>
              <input matInput formControlName="company" placeholder="Optional" />
              <mat-icon matSuffix>business</mat-icon>
            </mat-form-field>

            <mat-form-field appearance="outline" class="form-field">
              <mat-label>Phone</mat-label>
              <input matInput formControlName="phone" placeholder="Optional" />
              <mat-icon matSuffix>phone</mat-icon>
            </mat-form-field>
          </div>

          <mat-form-field appearance="outline" class="form-field full-width">
            <mat-label>Address</mat-label>
            <textarea matInput formControlName="address" rows="3" placeholder="Optional"></textarea>
            <mat-icon matSuffix>location_on</mat-icon>
          </mat-form-field>
        </form>
      </mat-card-content>

      <mat-card-actions align="end">
        <button mat-button (click)="cancelEdit()" [disabled]="loading">
          <mat-icon>close</mat-icon>
          Close
        </button>
        <button mat-raised-button color="primary" [disabled]="addForm.invalid || loading" (click)="isEditMode ? updateCustomer() : addCustomer()">
          <mat-icon *ngIf="!loading" [textContent]="isEditMode ? 'save' : 'add'"></mat-icon>
          <mat-spinner diameter="20" *ngIf="loading"></mat-spinner>
          <span [textContent]="isEditMode ? 'Save changes' : 'Create'"></span>
        </button>
      </mat-card-actions>
    </mat-card>

    <mat-card class="table-card" *ngIf="customers.length > 0">
      <mat-card-header>
        <mat-card-title>Customers</mat-card-title>
        <mat-card-subtitle [textContent]="dataSource.filteredData.length + ' shown'"></mat-card-subtitle>
        <div class="card-header-actions">
          <mat-form-field appearance="outline" class="filter-field">
            <mat-label>Search</mat-label>
            <input matInput [value]="filterValue" (input)="applyFilter($any($event.target).value)" placeholder="Name, email, company" />
            <button mat-icon-button matSuffix aria-label="Clear" *ngIf="filterValue" (click)="applyFilter('')">
              <mat-icon>close</mat-icon>
            </button>
          </mat-form-field>
          <button mat-raised-button color="primary" (click)="startAdd()" *ngIf="!showAddForm">
            <mat-icon>add</mat-icon>
            Add
          </button>
        </div>
      </mat-card-header>

      <mat-card-content>
        <div class="table-container">
          <table mat-table [dataSource]="dataSource" matSort class="customers-table">
            <ng-container matColumnDef="name">
              <th mat-header-cell *matHeaderCellDef mat-sort-header>Name</th>
              <td mat-cell *matCellDef="let customer">
                <div class="customer-name">
                  <mat-icon class="customer-avatar">person</mat-icon>
                  <span [textContent]="customer.name"></span>
                </div>
              </td>
            </ng-container>

            <ng-container matColumnDef="email">
              <th mat-header-cell *matHeaderCellDef mat-sort-header>Email</th>
              <td mat-cell *matCellDef="let customer"><span [textContent]="customer.email"></span></td>
            </ng-container>

            <ng-container matColumnDef="company">
              <th mat-header-cell *matHeaderCellDef mat-sort-header>Company</th>
              <td mat-cell *matCellDef="let customer"><span [textContent]="customer.company || '-'"></span></td>
            </ng-container>

            <ng-container matColumnDef="actions">
              <th mat-header-cell *matHeaderCellDef>Actions</th>
              <td mat-cell *matCellDef="let customer">
                <button mat-icon-button color="primary" (click)="editCustomer(customer)" aria-label="Edit">
                  <mat-icon>edit</mat-icon>
                </button>
                <button mat-icon-button color="warn" (click)="deleteCustomer(customer)" aria-label="Delete">
                  <mat-icon>delete</mat-icon>
                </button>
              </td>
            </ng-container>

            <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
            <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
          </table>

          <mat-paginator [pageSizeOptions]="[5, 10, 20]" showFirstLastButtons></mat-paginator>
        </div>
      </mat-card-content>
    </mat-card>

    <div *ngIf="loading && customers.length === 0" class="loading-container">
      <mat-spinner diameter="50"></mat-spinner>
      <p>Loading customers...</p>
    </div>
  </div>
</div>
=======

<mat-card class="customer-card">
  <mat-card-title>Customer Management</mat-card-title>
  <mat-card-subtitle>Full CRUD demo for customer management</mat-card-subtitle>

  <mat-divider></mat-divider>

  {% if error is defined %}
    <div *ngIf="error" class="error-message">
      <mat-icon color="warn">error</mat-icon> {{ error }}
    </div>
  {% endif %}
  <div *ngIf="loading" class="loading-message">
    <mat-progress-bar mode="indeterminate"></mat-progress-bar>
    Loading customers...
  </div>

  <form (ngSubmit)="addCustomer()" class="customer-form" autocomplete="off">
    <h3>Add Customer</h3>
    <mat-form-field appearance="outline" class="form-field">
      <mat-label>Name</mat-label>
      <input matInput type="text" [(ngModel)]="newCustomer.name" name="name" required />
    </mat-form-field>
    <mat-form-field appearance="outline" class="form-field">
      <mat-label>Email</mat-label>
      <input matInput type="email" [(ngModel)]="newCustomer.email" name="email" required />
    </mat-form-field>
    <mat-form-field appearance="outline" class="form-field">
      <mat-label>Company</mat-label>
      <input matInput type="text" [(ngModel)]="newCustomer.company" name="company" />
    </mat-form-field>
    <div class="form-actions">
      <button mat-raised-button color="primary" type="submit">Add</button>
    </div>
  </form>

  <div *ngIf="selectedCustomer" class="edit-form">
    <h3>Edit Customer</h3>
    <mat-form-field appearance="outline" class="form-field">
      <mat-label>Name</mat-label>
      <input matInput type="text" [(ngModel)]="selectedCustomer.name" name="editName" />
    </mat-form-field>
    <mat-form-field appearance="outline" class="form-field">
      <mat-label>Email</mat-label>
      <input matInput type="email" [(ngModel)]="selectedCustomer.email" name="editEmail" />
    </mat-form-field>
    <mat-form-field appearance="outline" class="form-field">
      <mat-label>Company</mat-label>
      <input matInput type="text" [(ngModel)]="selectedCustomer.company" name="editCompany" />
    </mat-form-field>
    <div class="form-actions">
      <button mat-raised-button color="accent" (click)="updateCustomer()">Save</button>
      <button mat-button (click)="selectedCustomer = null">Cancel</button>
    </div>
  </div>

  <mat-divider></mat-divider>

  {% raw %}
  <mat-list>
    <mat-list-item *ngFor="let customer of customers">
      <div class="customer-info">
        <span class="customer-name">{{ customer.name }}</span>
        <span class="customer-email">({{ customer.email }})</span>
        <span class="customer-company">- {{ customer.company }}</span>
      </div>
      <span class="spacer"></span>
      <button mat-icon-button color="primary" (click)="selectCustomer(customer)" matTooltip="Edit">
        <mat-icon>edit</mat-icon>
      </button>
      <button mat-icon-button color="warn" (click)="deleteCustomer(customer)" matTooltip="Delete">
        <mat-icon>delete</mat-icon>
      </button>
    </mat-list-item>
  </mat-list>
  {% endraw %}
</mat-card>
>>>>>>> cb0c5fb2 (Fix: Jinja2 template errors, update orchestrator, and sync Angular templates)
